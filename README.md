# This is for cms management package

## This is to create custom package for cms management.

Install via composer
composer require csoftech/cms

Then publish it 
php artisan vendor:publish

Then run migration
php artisan migrate